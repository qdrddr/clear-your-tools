$kind: http-request
url: "{{base_url}}/mcp"
method: POST
headers:
  Accept: application/json, text/event-stream
  Content-Type: application/json
  mcp-session-id: "{{mcp_session_id}}"
body:
  type: json
  content: |
    {
      "jsonrpc": "2.0",
      "id": 2,
      "method": "tools/list",
      "params": {}
    }
examples: .resources/03 - POST tools list.resources/examples
order: 3003
