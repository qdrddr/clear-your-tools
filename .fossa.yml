# FOSSA: scan production Python deps only (see legal/POLICY.md).
# requirements-dev.txt and uv.lock are excluded so dev/CI copyleft (e.g. LGPL) does not fail the release gate.

version: 3

paths:
  exclude:
    - requirements-dev.txt

targets:
  exclude:
    - type: uv
      path: .
