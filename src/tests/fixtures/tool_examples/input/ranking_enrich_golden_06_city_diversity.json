{
  "query_id": "06_city_diversity",
  "query": "city address lookup",
  "tool_names": [
    "geo_lookup"
  ],
  "tools": [
    {
      "name": "geo_lookup",
      "description": "Look up geographic locations.",
      "server_key": "geo",
      "tool_name": "lookup",
      "input_schema": {
        "type": "object",
        "properties": {
          "city": {
            "type": "string",
            "description": "City name or city with state."
          }
        },
        "required": [
          "city"
        ]
      }
    }
  ],
  "flattened_examples": [],
  "inject": {
    "max_per_property": 3,
    "max_value_chars": 120,
    "full_call_examples": false,
    "cross_schema_fallback": false,
    "ranking": {
      "pipeline": [
        "bm25"
      ],
      "rrf_k": 60,
      "rerank_min_candidates": 3,
      "llm_min_candidates": 5,
      "diversity_threshold": 0.6
    }
  }
}
