name: SDK C/Go

on:
  push:
    branches: [main]
    paths:
      - sdk/rust/cyt-indexer/**
      - sdk/c/**
      - sdk/c/scripts/**
      - sdk/go/**
      - Cargo.toml
      - .github/workflows/sdk-c-go.yml
  pull_request:
    paths:
      - sdk/rust/cyt-indexer/**
      - sdk/c/**
      - sdk/c/scripts/**
      - sdk/go/**
      - Cargo.toml
      - .github/workflows/sdk-c-go.yml

concurrency:
  group: sdk-c-go-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sdk-c-go:
    name: C + Go (${{ matrix.target }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            goarch: amd64
          - runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            goarch: arm64
          - runner: macos-latest
            target: aarch64-apple-darwin
            goarch: arm64
          - runner: macos-latest
            target: x86_64-apple-darwin
            goarch: amd64
          - runner: windows-latest
            target: x86_64-pc-windows-msvc
            goarch: amd64
          - runner: windows-11-arm
            target: aarch64-pc-windows-msvc
            goarch: arm64

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.25"
          cache: false

      - name: Install uv (parity tests)
        uses: astral-sh/setup-uv@d31148d669074a8d0a63714ba94f3201e7020bc3
        with:
          python-version: "3.13"
          enable-cache: true

      - name: Sync Python SDK (parity reference)
        run: uv sync --all-extras --group dev --group test --locked

      - name: Build shared C library
        shell: bash
        run: bash sdk/c/scripts/build-c-lib.sh --target ${{ matrix.target }}

      - name: Rust FFI tests
        shell: bash
        run: |
          cargo test -p cyt-indexer --no-default-features --features ffi \
            --target ${{ matrix.target }}

      - name: Configure CMake (sdk/c)
        shell: bash
        run: |
          extra_args=()
          case "${{ matrix.target }}" in
            x86_64-apple-darwin) extra_args=(-DCMAKE_OSX_ARCHITECTURES=x86_64) ;;
            aarch64-apple-darwin) extra_args=(-DCMAKE_OSX_ARCHITECTURES=arm64) ;;
          esac
          cmake -S sdk/c -B sdk/c/build -DCMAKE_BUILD_TYPE=Release \
            -DCYT_RUST_TARGET=${{ matrix.target }} "${extra_args[@]}"

      - name: Build and test sdk/c
        shell: bash
        run: |
          cmake --build sdk/c/build
          case "${{ matrix.target }}" in
            x86_64-apple-darwin)
              arch -x86_64 ctest --test-dir sdk/c/build --output-on-failure
              ;;
            *)
              ctest --test-dir sdk/c/build --output-on-failure
              ;;
          esac

      - name: Go tests (sdk/go)
        shell: bash
        working-directory: sdk/go
        env:
          CGO_ENABLED: "1"
          GOARCH: ${{ matrix.goarch }}
        run: |
          case "${{ matrix.target }}" in
            x86_64-apple-darwin) arch -x86_64 go test -v ./... ;;
            *) go test -v ./... ;;
          esac

      - name: Verify cyt-native-ensure bootstrap
        shell: bash
        working-directory: sdk/go
        env:
          CGO_ENABLED: "1"
          GOARCH: ${{ matrix.goarch }}
        run: |
          # Use monorepo-built artifacts; -force would re-download from GitHub Releases.
          case "${{ matrix.target }}" in
            x86_64-apple-darwin)
              arch -x86_64 go run ./cmd/cyt-native-ensure
              arch -x86_64 go test -count=1 ./...
              ;;
            *)
              go run ./cmd/cyt-native-ensure
              go test -count=1 ./...
              ;;
          esac
