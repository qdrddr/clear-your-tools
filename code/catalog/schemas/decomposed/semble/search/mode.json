{
  "name": "search",
  "inputSchema": {
    "properties": {
      "mode": {
        "default": "hybrid",
        "description": "Search mode. 'hybrid' is best for most queries.",
        "enum": [
          "hybrid",
          "semantic",
          "bm25"
        ],
        "title": "Mode",
        "type": "string"
      }
    }
  }
}