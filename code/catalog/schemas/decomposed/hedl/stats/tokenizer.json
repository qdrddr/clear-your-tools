{
  "name": "hedl_stats",
  "inputSchema": {
    "properties": {
      "tokenizer": {
        "type": "string",
        "enum": [
          "cl100k",
          "simple"
        ],
        "description": "Tokenizer to use (cl100k for GPT-4, simple for approximate count)",
        "default": "simple"
      }
    }
  }
}