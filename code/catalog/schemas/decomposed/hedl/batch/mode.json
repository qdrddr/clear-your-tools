{
  "name": "batch",
  "inputSchema": {
    "properties": {
      "mode": {
        "type": "string",
        "enum": [
          "continue_on_error",
          "stop_on_error"
        ],
        "description": "Execution mode",
        "default": "continue_on_error"
      }
    }
  }
}