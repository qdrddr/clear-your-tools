{
  "name": "hedl_write",
  "description": "Write HEDL content to a file. Supports writing optimized data back to disk after processing.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Path to write the HEDL file (relative to root directory)"
      },
      "content": {
        "type": "string",
        "description": "HEDL content to write"
      }
    },
    "required": [
      "path",
      "content"
    ]
  }
}