{
  "name": "hedl_convert_from",
  "description": "Convert other formats to HEDL: json, yaml, xml, csv, parquet, toon.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "content": {
        "type": "string",
        "description": "Content to convert (base64 for parquet)"
      },
      "format": {
        "type": "string",
        "enum": [],
        "description": "Source format"
      }
    },
    "required": [
      "content",
      "format"
    ]
  }
}