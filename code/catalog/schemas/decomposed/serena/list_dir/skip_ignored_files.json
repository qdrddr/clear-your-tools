{
  "name": "list_dir",
  "inputSchema": {
    "properties": {
      "skip_ignored_files": {
        "default": false,
        "title": "Skip Ignored Files",
        "type": "boolean",
        "description": "Whether to skip files and directories that are ignored."
      }
    }
  }
}